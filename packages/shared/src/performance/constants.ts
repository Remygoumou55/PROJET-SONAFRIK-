/** CDC V9.0 — animations UI max 300ms (hors landing marketing public) */
export const CDC_UI_ANIMATION_MAX_MS = 300;

export const SEARCH_RESULT_CACHE_TTL_MS = 5 * 60 * 1000;

/** Durées count-up par défaut (marketing / dashboard) */
export const COUNT_UP_DURATION_DEFAULT_MS = 1500;
export const ANIMATED_NUMBER_DURATION_DEFAULT_MS = 1200;
