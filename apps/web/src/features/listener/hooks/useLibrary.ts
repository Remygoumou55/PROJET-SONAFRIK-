"use client";

/**
 * @deprecated Utiliser `useLibraryLdse()` dans `LibraryLdseProvider`.
 * Conservé pour compatibilité — redirige vers le contexte LDSE si présent.
 */
export { useLibraryLdse as useLibrary } from "../lib/libraryLdseContext";
