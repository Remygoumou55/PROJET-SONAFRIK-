"use client";

import "@/app/styles/listen-future.css";

/** Charge le CSS des fonctionnalités futures uniquement quand un flag /listen est actif. */
export function ListenFutureStyles() {
  return null;
}
