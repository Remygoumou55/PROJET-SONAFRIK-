import { AdminModulePlaceholder } from "@/features/admin/components/AdminModulePlaceholder";

export const metadata = { title: "Analytiques — Admin SONAFRIK" };

export default function AdminAnalyticsPage() {
  return <AdminModulePlaceholder title="Analytiques" sprintLabel="Sprint Admin 5" icon="📈" />;
}
