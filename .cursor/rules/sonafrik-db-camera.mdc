---
description: SONAFRIK database surveillance — tables réelles, RLS, migrations, incohérences connues
alwaysApply: true
---

# Caméra Base de Données SONAFRIK

## Projet Supabase : `cxjpburiiazzvlczzupy`

## DB live vérifiée le 2026-06-21 : 53 tables, TOUTES avec RLS=true

Tables confirmées en production :
`admin_notifications` · `album_genres` · `albums` · `artist_profiles` · `audit_logs`
`beat_purchases` · `beats` · `contracts` · `contributors` · `creator_roles`
`creator_verifications` · `creators` · `favorites` · `feature_flags` · `follows`
`genres` · `label_members` · `labels` · `notifications` · `ownership_versions`
`ownerships` · `payment_intents` · `payout_accounts` · `payout_audit_logs` · `payout_batches`
`permissions` · `playback_positions` · `playlist_tracks` · `playlists` · `profiles`
`rate_limits` · `rights_claims` · `role_permissions` · `roles` · `royalty_calculations`
`royalty_cycles` · `stream_events` · `stream_sessions` · `studios` · `system_settings`
`tips` · `track_credits` · `track_files` · `track_genres` · `tracks`
`transactions` · `user_preferences` · `user_roles` · `user_sessions` · `wallet_ledger`
`wallets` · `withdrawals` · `works`

## Tables ORPHELINES (dans DB mais pas utilisées par packages/api)

- `creators` — Doublon de `artist_profiles` ? Relation inconnue
- `studios` — Feature non implémentée dans packages/api
- `creator_roles` — RBAC créateur non connecté
- `label_members` — packages/api utilise le vieux nom `team_members` qui n'existe PAS en DB
- `labels` — non référencée dans packages/api

## Table MANQUANTE (seule absente)

- `subscription_plans` — CONFIRMÉE ABSENTE de la DB live — à créer avant paiements

## Incohérences documentées dans ADMIN_GUIDE.md

| Ce que dit ADMIN_GUIDE | Réalité en DB |
|---|---|
| `audit_log` | Table = `audit_logs` (avec s) |
| `subscription_plans` | Table n'existe PAS |
| `artist_profiles.user_id` | Colonne = `creator_id` |

## Types DB périmés → cause des 39 as never

```bash
# Régénérer les types TypeScript depuis la DB live
supabase gen types typescript --project-id cxjpburiiazzvlczzupy \
  --schema public > packages/database/src/types/index.ts
```

## Workflow migrations SQL (OBLIGATOIRE)

1. Écrire SQL dans `supabase/migrations/YYYYMMDDHHMMSS_<nom>.sql`
2. `supabase db query --linked --file supabase/migrations/<fichier>.sql`
3. Valider : `supabase db query --linked "SELECT tablename FROM pg_tables WHERE schemaname='public'"`
4. Commit + push
