---
description: SONAFRIK security surveillance — BYPASS_AUTH, AsyncStorage, hex, clés API, failles connues
alwaysApply: true
---

# Caméra Sécurité SONAFRIK

## État sécurité actuel (Jun 2026)

### ✅ Correct
- `BYPASS_AUTH` bloqué sur Vercel dans middleware.ts, requireAdmin.ts, requireCreator.ts, requireIdentity.ts
- `moment.js` supprimé (0 imports)
- `console.log` = 0 dans apps/ (aucune fuite de données en prod)
- `as any` = 1 occurrence isolée (admin/health/page.tsx — @ts-ignore intentionnel)
- Error boundaries = 7 fichiers couvrant tous les route groups

### ⚠️ Risques actifs à corriger

| Risque | Fichier | Priorité |
|---|---|---|
| AsyncStorage non chiffré pour auth tokens | `apps/mobile/lib/supabase.ts` | HAUTE |
| Aucun guard auth sur `(tabs)/_layout.tsx` mobile | `apps/mobile/app/(tabs)/_layout.tsx` | HAUTE |
| CORS `"*"` fallback sur 11 edge functions | `supabase/functions/*/index.ts` | HAUTE |
| 4 opérateurs paiement = TODO stubs (0 intégration réelle) | `supabase/functions/payment-initiate/index.ts` | CRITIQUE |
| `subscription_plans` table absente de DB live | supabase/migrations/ | CRITIQUE |
| Texte "Sprint 2 — Auth OTP SMS" en production mobile | `apps/mobile/app/index.tsx` | HAUTE |
| 5 artistes hardcodés fictifs sur page lancement | `apps/web/src/app/lancement/page.tsx` | MOYENNE |
| 0 tests (aucun .test.ts ni .spec.ts) | partout | HAUTE |
| CSP unsafe-eval + unsafe-inline en production | `apps/web/next.config.ts` | MOYENNE |
| URL signées audio = 7200s (2h) — trop long | `supabase/functions/stream-start/index.ts` | BASSE |

## AsyncStorage → SecureStore (À faire)

```typescript
// ❌ ACTUEL (apps/mobile/lib/supabase.ts)
import AsyncStorage from "@react-native-async-storage/async-storage";
storage: AsyncStorage, // stockage clair, non chiffré

// ✅ CORRECT (à implémenter)
import * as SecureStore from "expo-secure-store";
const ExpoSecureStoreAdapter = {
  getItem: (key: string) => SecureStore.getItemAsync(key),
  setItem: (key: string, value: string) => SecureStore.setItemAsync(key, value),
  removeItem: (key: string) => SecureStore.deleteItemAsync(key),
};
storage: ExpoSecureStoreAdapter,
```

## Règles absolues

- ❌ NEVER committer .env, .env.local, secrets
- ❌ NEVER service_role key côté client
- ❌ NEVER BYPASS_AUTH sur Vercel (VERCEL=1)
- ❌ NEVER git push --force sur main
- ❌ NEVER --no-verify sur pre-commit hooks
